name: Deploy Helm Chart

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:

      - name: Checkout code
        uses: actions/checkout@v2

      - name: Helm tool installer
        uses: Azure/setup-helm@v3

      - name: Check Helm version
        run: helm version

      - name: Generate Random Semantic Version
        id: random-version
        run: echo "::set-output name=version::$(echo v0.$((1 + RANDOM % 9)).$((1 + RANDOM % 9)))-${{ github.run_number }}"
        
      - name: Package Helm Chart
        run: |
          helm package --version ${{ steps.random-version.outputs.version }} .

      - name: Update index
        run: |
          helm repo index .
          git config --local user.email "actions@github.com"
          git config --local user.name "GitHub Actions"
          git add index.yaml
          git commit -m "feat: Update chart index"
          # git remote set-url origin https://github.com/dinushchathurya/tomcat-helm-multi-env
          git push https://dinushchathurya:${{secrets.PAT_TOKEN}}@github.com/dinushchathurya/tomcat-helm-multi-env.git HEAD:master -f
          # git push origin master 
        # env:
        #   PAT: ${{ secrets.PAT_TOKEN }}





